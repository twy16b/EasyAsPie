<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.fsu.cs.easyaspie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".TimerReciever"
            android:enabled="true"
            android:exported="true"></receiver>

        <service
            android:name=".StartTimerService"
            android:exported="false" />

        <activity android:name=".RecipeSteps" />
        <activity android:name=".RecipeActivity" />
        <activity android:name=".RecipeListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".RecipesProvider"
            android:authorities="edu.fsu.cs.easyaspie.RecipesProvider"
            android:exported="false"
            android:readPermission=""
            android:writePermission="" />
    </application>

</manifest>